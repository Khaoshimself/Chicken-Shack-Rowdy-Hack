#ifndef HEADER_H
#define HEADER_H
  
typedef struct{
  int p1, p2, p3, p4;
}
 
bool cardCount(int count);
int dealCard();
int calScore(int[]);
void playerInfo(int p1, int p2, int p3, int p4);

#endif
